import { StyleColorDirective } from './style-color.directive';

describe('StyleColorDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleColorDirective();
    expect(directive).toBeTruthy();
  });
});
